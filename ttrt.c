#include <stdio.h>

for(;;)
{
	call_some_function_with_paramater(12, 17);
	if(function.callback == TRUE)
	{
		other_function(1);
		something_else(3);
	}
	else
	{
		other_function(6);
		hopsasa(5);
	}

}

return 0; // should never execute
